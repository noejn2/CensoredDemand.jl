# macOS
.DS_Store

# Julia — library convention: do not commit the Manifest
Manifest.toml

# Coverage / build artifacts
*.cov
*.jl.cov
*.jl.*.cov
/deps/build.log
/docs/build/

# Publication pipeline — separate downstream env (own Project/Manifest), plus
# ~32 MB of survey data / figures / build artifacts. NOT part of the registered
# package; kept out of the repo entirely.
/pub/
